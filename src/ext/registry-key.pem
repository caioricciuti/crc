# The public key official extension registries are signed with, as PEM
# (-----BEGIN PUBLIC KEY----- ...). It is the same file as keys/registry.pub.pem
# in crc-extensions. Until one is here, this build trusts no registry:
# installing from a folder still works, unsigned, with a warning.
